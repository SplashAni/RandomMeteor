package random.meteor.util.player;

public enum RotationType {
    Client, // rotates cam (default mc rotations basically)
    Server, // rotates but the main cam isnt affected but renders in first person!!!!
    Silent, // sends rots packts only
    None // no ratiotns BOMBACLATT  /*ps : making notes for self here lOL*/

}
